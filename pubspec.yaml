name: facsimile
description: This pac is to remouve background from pics and photos of your stamp or sign to make facsimiles to documents.
version: 0.0.3
repository: https://github.com/zigbug/facsimile

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  image: ^4.0.17
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

  